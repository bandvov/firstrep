export function movie(data) {
    const html = `<article class="movie" 
    <h2>${data.title}</h2>
    <div>${data.country}</div>
    <div>${data.imgSrc}</div>
    <div>${overview}</div>
    </article>`
}